import Image from 'next/image'

interface Props {
    imgsrc: string;
    heading: React.ReactNode;
}

// Based on https://tailwind-elements.com/docs/standard/designblocks/hero-sections/

export default function SignupForm({ imgsrc, heading }: Props) {
    return <div>
    </div>;
}